Smart Proxy has been installed as release "{{ .Release.Name }}" in namespace "{{ .Release.Namespace }}".

1. Check that the pod is running:
   kubectl get pods -n {{ .Release.Namespace }} -l "app.kubernetes.io/instance={{ .Release.Name }}"

{{- if .Values.route.enabled }}

2. Admin dashboard (OpenShift Route):
   https://{{ .Values.route.host }}
{{- else if .Values.ingress.enabled }}

2. Admin dashboard (Ingress):
   http{{ if .Values.ingress.tls }}s{{ end }}://{{ .Values.ingress.host }}
{{- else }}

2. The admin dashboard is not exposed externally. Port-forward to reach it:
   kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "smart-proxy.fullname" . }} 8081:{{ .Values.service.adminPort }}
   Then open http://localhost:8081
{{- end }}

See charts/smart-proxy/values.yaml for all configuration options.
